需要修正的内容：

1. T Force = Temporary Force（临时强制调整）

2. 参数调整方式：
   - 不是自由输入值
   - 而是 true/false 开关
   - 对应 URL 中是否包含该参数
   - ✓ (true) → URL 中出现 key=true
   - ✗ (false) → URL 中不包含该参数

3. UI 应该使用 Toggle 开关而不是输入框
